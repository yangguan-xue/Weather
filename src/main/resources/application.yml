server:
  port: 8080
  servlet:
    context-path: /api

spring:
  application:
    name: weatherReport

  # MySQL?????
  datasource:
    url: jdbc:mysql://localhost:3306/Weather?useSSL=false&serverTimezone=UTC&characterEncoding=utf8
    username: root
    password: ygx28YGX
    driver-class-name: com.mysql.cj.jdbc.Driver
    hikari:
      maximum-pool-size: 10
      minimum-idle: 5
      connection-timeout: 30000

  # JPA??
  jpa:
    hibernate:
      ddl-auto: update  # ????/?????
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect
        format_sql: true
    open-in-view: false

  wechat:
    appid: wx7e32dcfe7de20fbd
    secret: 7f1599cc42919be9098c71dc3d2d6f29
    url:
      code2session: https://api.weixin.qq.com/sns/jscode2session

# ????
logging:
  level:
    root: INFO
    com.weather: DEBUG
    org.hibernate.SQL: DEBUG
    org.hibernate.type.descriptor.sql.BasicBinder: TRACE
  pattern:
    console: "%d{yyyy-MM-dd HH:mm:ss} [%thread] %-5level %logger{36} - %msg%n"

security:
  basic:
    enabled: false\

jwt:
    secret: biaGG8kPHIc4xphP+l0mWx4ZRL7rQQ1q9fEVRVvIewDKh1mmoT3aYTDmU2hZ7yeG
    expiration: 7200